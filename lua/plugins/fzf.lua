return {
  { -- fzf use external toolkits (snacks.nvim use internal functions)
    -- , so it supports a few functions not supported by snacks.nvim
    "ibhagwan/fzf-lua",
    opts = { "skim" }
  }
}
